# Seu Metrô — Implementação v1

> **Para agentic workers:** SUB-SKILL OBRIGATÓRIO: use `superpowers:subagent-driven-development` (recomendado) ou `superpowers:executing-plans` para implementar tarefa por tarefa. Passos usam checkbox (`- [ ]`).

**Goal:** Entregar o app mobile Flutter "Seu Metrô" com mapa geográfico das estações, planejador de rotas, status ao vivo das linhas, horários tabelados, estação mais próxima e favoritos locais.

**Architecture:** App Flutter 100% cliente com padrão de repositórios. Dados estáticos (linhas/estações/horários) em JSON embutidos em `assets/data/`. Status ao vivo consumido da API pública do Metrô SP (`apim-proximotrem...azure-api.net`), com cache em memória e modo offline. Rotas via grafo + A* com penalidade de baldeação. Estado gerenciado com Riverpod.

**Tech Stack:** Flutter 3.44+, Dart 3.12+, `flutter_riverpod`, `dio`, `geolocator`, `flutter_map`, `latlong2`, `shared_preferences`, `google_fonts`.

## Global Constraints

- Linguagem da UI: **pt-BR**.
- Nome do app: **Seu Metrô** (nome do package: `seu_metro`, org `br.com.seumetro`).
- Cores das linhas = hex oficiais (abaixo). Nunca inventar cores.
- Toda mensagem de erro ao usuário em pt-BR e amigável; nunca tela branca.
- Projeto não é repo git hoje — Task 1 inicializa git e commita a cada tarefa.
- Verificação a cada tarefa: `flutter analyze` sem erros novos + `flutter test` passando.
- API de status: `GET https://apim-proximotrem-prd-brazilsouth-001.azure-api.net/api/v1/lines`. Sem chave. Resposta: `{"Data":[{Code, ColorName, ColorHex, Line, StatusCode, StatusLabel, StatusColor, Description}]}`.
- Dados estáticos ficam em `assets/data/*.json` declarados no `pubspec.yaml`.

---

### Task 1: Scaffold do projeto Flutter + shell de navegação

**Files:**
- Create: projeto Flutter em `./` (raiz `C:\Users\luan\Meu_Buss`)
- Create: `lib/main.dart`, `lib/features/home/home_shell.dart`, `lib/features/map/map_page.dart`, `lib/features/routes/routes_page.dart`, `lib/features/status/status_page.dart`, `lib/features/schedules/schedules_page.dart`, `lib/features/favorites/favorites_page.dart`
- Modify: `pubspec.yaml`
- Test: `test/widget/home_shell_test.dart`

**Interfaces:**
- Produces: `HomeShell` (widget com `NavigationBar` e 5 destinos: Mapa, Rotas, Status, Horários, Favoritos). As 5 páginas são placeholders temporários que exibem o nome da tela (serão implementadas nas Tasks 9-13).

- [ ] **Step 1: Criar o projeto Flutter na raiz**

Run (na raiz `C:\Users\luan\Meu_Buss`):
```powershell
flutter create --project-name seu_metro --org br.com.seumetro --platforms android,ios .
```

- [ ] **Step 2: Inicializar git e commit inicial**

```powershell
git init
git add -A
git commit -m "chore: scaffold flutter project seu_metro"
```

- [ ] **Step 3: Adicionar dependências no `pubspec.yaml`**

Em `pubspec.yaml`, seção `dependencies` (manter as já geradas pelo `flutter create`):

```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  geolocator: ^13.0.2
  flutter_map: ^8.1.0
  latlong2: ^0.9.1
  shared_preferences: ^2.3.4
  google_fonts: ^6.2.1
```

E a seção de assets (adicionar):

```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/data/
```

Run: `flutter pub get`

- [ ] **Step 4: Substituir `lib/main.dart`**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'features/home/home_shell.dart';

void main() {
  runApp(const ProviderScope(child: SeuMetroApp()));
}

class SeuMetroApp extends StatelessWidget {
  const SeuMetroApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Seu Metrô',
      debugShowCheckedModeBanner: false,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: const Color(0xFF00378C),
          brightness: Brightness.light,
        ),
        useMaterial3: true,
      ),
      darkTheme: ThemeData(
        colorScheme: ColorScheme.fromSeed(
          seedColor: const Color(0xFF00378C),
          brightness: Brightness.dark,
        ),
        useMaterial3: true,
      ),
      home: const HomeShell(),
    );
  }
}
```

- [ ] **Step 5: Criar as 5 páginas placeholder e o shell**

Criar cada `*_page.dart` com o mesmo padrão (exemplo para mapa):

```dart
import 'package:flutter/material.dart';

class MapPage extends StatelessWidget {
  const MapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Mapa')));
  }
}
```

Criar `lib/features/home/home_shell.dart`:

```dart
import 'package:flutter/material.dart';

import '../favorites/favorites_page.dart';
import '../map/map_page.dart';
import '../routes/routes_page.dart';
import '../schedules/schedules_page.dart';
import '../status/status_page.dart';

class HomeShell extends StatefulWidget {
  const HomeShell({super.key});

  @override
  State<HomeShell> createState() => _HomeShellState();
}

class _HomeShellState extends State<HomeShell> {
  int _index = 0;

  static const _pages = [
    MapPage(),
    RoutesPage(),
    StatusPage(),
    SchedulesPage(),
    FavoritesPage(),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: IndexedStack(index: _index, children: _pages),
      bottomNavigationBar: NavigationBar(
        selectedIndex: _index,
        onDestinationSelected: (i) => setState(() => _index = i),
        destinations: const [
          NavigationDestination(icon: Icon(Icons.map_outlined), label: 'Mapa'),
          NavigationDestination(icon: Icon(Icons.route_outlined), label: 'Rotas'),
          NavigationDestination(icon: Icon(Icons.sensors_outlined), label: 'Status'),
          NavigationDestination(icon: Icon(Icons.schedule_outlined), label: 'Horários'),
          NavigationDestination(icon: Icon(Icons.star_outline), label: 'Favoritos'),
        ],
      ),
    );
  }
}
```

- [ ] **Step 6: Escrever o teste de widget**

`test/widget/home_shell_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/home/home_shell.dart';

void main() {
  testWidgets('HomeShell mostra 5 destinos de navegação', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: MaterialApp(home: HomeShell())));
    expect(find.byType(NavigationBar), findsOneWidget);
    expect(find.text('Mapa'), findsOneWidget);
    expect(find.text('Rotas'), findsOneWidget);
    expect(find.text('Status'), findsOneWidget);
    expect(find.text('Horários'), findsOneWidget);
    expect(find.text('Favoritos'), findsOneWidget);
  });
}
```

- [ ] **Step 7: Rodar o teste para ver passar**

Run: `flutter test test/widget/home_shell_test.dart`
Expected: PASS (2 testes de exemplo do template precisam ser removidos — deletar `test/widget_test.dart`).

- [ ] **Step 8: Analisar e verificar**

Run: `flutter analyze` → sem erros. `flutter test` → verde.

- [ ] **Step 9: Commit**

```powershell
git add -A
git commit -m "feat: scaffold app com shell de navegação de 5 abas"
```

---

### Task 2: Modelos de domínio + parsing

**Files:**
- Create: `lib/models/line.dart`, `lib/models/station.dart`, `lib/models/line_status.dart`, `lib/models/line_schedule.dart`, `lib/models/route.dart`
- Test: `test/models/parsing_test.dart`

**Interfaces:**
- Consumes: nada.
- Produces (assinaturas exatas usadas nas próximas tarefas):
  - `class Line { final String id; final String name; final int colorValue; final String operator; final String terminalA; final String terminalB; final List<String> stationIds; Line.fromJson(Map<String, dynamic> json); }` — `stationIds` é a lista ORDENADA (ordem de viagem) dos ids de estação da linha; é ela que define as arestas do grafo.
  - `class Station { final String id; final String name; final double lat; final double lon; final List<String> lineIds; Station.fromJson(...); }`
  - `class LineStatus { final String lineId; final String statusCode; final String statusLabel; final String statusColor; final String? description; final DateTime updatedAt; LineStatus.fromJson(int code, Map<String, dynamic> json); }`
  - `class LineSchedule { final String lineId; final String direction; final String terminal; final String firstTrain; final String lastTrain; final int headwayPeakMin; final int headwayNormalMin; LineSchedule.fromJson(...); }`
  - `class RouteLeg { final String lineId; final String directionTerminal; final String fromStationId; final String toStationId; final int stationCount; final int minutes; }`
  - `class RoutePlan { final List<RouteLeg> legs; final int totalMinutes; final List<String> transferStationNames; }`

Nota de design: o `id` de estação é `l<linha>_<slug>` (ex.: `l1_tucuruvi`); estações de baldeação repetem o mesmo `id` com `lineIds` múltiplos (ex.: `l1_paraiso` e `l2_paraiso` são o MESMO id `paraiso`?). **Decisão**: id da estação é o **nome slug** (ex.: `paraiso`), sem prefixo de linha; `lineIds` lista as linhas. Isso permite mapear baldeação por igualdade de `id`.

- [ ] **Step 1: Escrever o teste de parsing (failing)**

`test/models/parsing_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/models/line.dart';
import 'package:seu_metro/models/line_schedule.dart';
import 'package:seu_metro/models/line_status.dart';
import 'package:seu_metro/models/station.dart';

void main() {
  test('Line.fromJson parseia os campos', () {
    final line = Line.fromJson({
      'id': '1', 'name': 'Linha 1 - Azul', 'colorHex': '#00378c',
      'operator': 'metro', 'terminalA': 'Jabaquara', 'terminalB': 'Tucuruvi',
      'stations': ['jabaquara', 'conceicao', 'tucuruvi'],
    });
    expect(line.id, '1');
    expect(line.name, 'Linha 1 - Azul');
    expect(line.colorValue, 0xFF00378C);
    expect(line.operator, 'metro');
    expect(line.terminalA, 'Jabaquara');
    expect(line.stationIds, ['jabaquara', 'conceicao', 'tucuruvi']);
  });

  test('Station.fromJson parseia os campos', () {
    final station = Station.fromJson({
      'id': 'paraiso', 'name': 'Paraíso', 'lat': -23.574, 'lon': -46.642,
      'lines': ['1', '2'],
    });
    expect(station.id, 'paraiso');
    expect(station.lineIds, ['1', '2']);
  });

  test('LineStatus.fromJson mapeia StatusColor', () {
    final status = LineStatus.fromJson(1, {
      'Code': 1, 'StatusLabel': 'Operação Normal', 'StatusCode': 'OperacaoNormal',
      'StatusColor': 'verde', 'Description': null,
    });
    expect(status.statusCode, 'OperacaoNormal');
    expect(status.statusColor, 'verde');
  });

  test('LineSchedule.fromJson parseia horários', () {
    final sched = LineSchedule.fromJson({
      'lineId': '1', 'direction': 'A', 'terminal': 'Jabaquara',
      'firstTrain': '04:40', 'lastTrain': '00:29',
      'headwayPeakMin': 2, 'headwayNormalMin': 6,
    });
    expect(sched.firstTrain, '04:40');
    expect(sched.headwayPeakMin, 2);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/models/parsing_test.dart`
Expected: FAIL ("Class 'Line' has no instance getter..." / não compila).

- [ ] **Step 3: Implementar os modelos**

`lib/models/line.dart`:

```dart
class Line {
  final String id;
  final String name;
  final int colorValue;
  final String operator;
  final String terminalA;
  final String terminalB;
  final List<String> stationIds;

  const Line({
    required this.id,
    required this.name,
    required this.colorValue,
    required this.operator,
    required this.terminalA,
    required this.terminalB,
    required this.stationIds,
  });

  factory Line.fromJson(Map<String, dynamic> json) {
    return Line(
      id: json['id'] as String,
      name: json['name'] as String,
      colorValue: _parseHex(json['colorHex'] as String),
      operator: json['operator'] as String,
      terminalA: json['terminalA'] as String,
      terminalB: json['terminalB'] as String,
      stationIds: (json['stations'] as List<dynamic>).cast<String>(),
    );
  }

  static int _parseHex(String hex) {
    final cleaned = hex.replaceFirst('#', '');
    return int.parse('FF$cleaned', radix: 16);
  }
}
```

`lib/models/station.dart`:

```dart
class Station {
  final String id;
  final String name;
  final double lat;
  final double lon;
  final List<String> lineIds;

  const Station({
    required this.id,
    required this.name,
    required this.lat,
    required this.lon,
    required this.lineIds,
  });

  factory Station.fromJson(Map<String, dynamic> json) {
    return Station(
      id: json['id'] as String,
      name: json['name'] as String,
      lat: (json['lat'] as num).toDouble(),
      lon: (json['lon'] as num).toDouble(),
      lineIds: (json['lines'] as List<dynamic>).cast<String>(),
    );
  }
}
```

`lib/models/line_status.dart`:

```dart
class LineStatus {
  final String lineId;
  final String statusCode;
  final String statusLabel;
  final String statusColor;
  final String? description;
  final DateTime updatedAt;

  const LineStatus({
    required this.lineId,
    required this.statusCode,
    required this.statusLabel,
    required this.statusColor,
    required this.description,
    required this.updatedAt,
  });

  factory LineStatus.fromJson(int code, Map<String, dynamic> json) {
    return LineStatus(
      lineId: '$code',
      statusCode: json['StatusCode'] as String? ?? 'unknown',
      statusLabel: json['StatusLabel'] as String? ?? 'Indisponível',
      statusColor: json['StatusColor'] as String? ?? 'cinza',
      description: json['Description'] as String?,
      updatedAt: DateTime.now(),
    );
  }
}
```

`lib/models/line_schedule.dart`:

```dart
class LineSchedule {
  final String lineId;
  final String direction; // 'A' ou 'B'
  final String terminal;
  final String firstTrain;
  final String lastTrain;
  final int headwayPeakMin;
  final int headwayNormalMin;

  const LineSchedule({
    required this.lineId,
    required this.direction,
    required this.terminal,
    required this.firstTrain,
    required this.lastTrain,
    required this.headwayPeakMin,
    required this.headwayNormalMin,
  });

  factory LineSchedule.fromJson(Map<String, dynamic> json) {
    return LineSchedule(
      lineId: json['lineId'] as String,
      direction: json['direction'] as String,
      terminal: json['terminal'] as String,
      firstTrain: json['firstTrain'] as String,
      lastTrain: json['lastTrain'] as String,
      headwayPeakMin: json['headwayPeakMin'] as int,
      headwayNormalMin: json['headwayNormalMin'] as int,
    );
  }
}
```

`lib/models/route.dart`:

```dart
class RouteLeg {
  final String lineId;
  final String directionTerminal;
  final String fromStationId;
  final String toStationId;
  final int stationCount;
  final int minutes;

  const RouteLeg({
    required this.lineId,
    required this.directionTerminal,
    required this.fromStationId,
    required this.toStationId,
    required this.stationCount,
    required this.minutes,
  });
}

class RoutePlan {
  final List<RouteLeg> legs;
  final int totalMinutes;
  final List<String> transferStationNames;

  const RoutePlan({
    required this.legs,
    required this.totalMinutes,
    required this.transferStationNames,
  });
}
```

- [ ] **Step 4: Rodar o teste para ver passar**

Run: `flutter test test/models/parsing_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

Run: `flutter analyze` → limpo.
```powershell
git add -A
git commit -m "feat: modelos de domínio com parsing"
```

---

### Task 3: Dados estáticos (lines.json, stations.json, schedules.json)

**Files:**
- Create: `assets/data/lines.json`, `assets/data/stations.json`, `assets/data/schedules.json`
- Test: `test/data/static_data_test.dart`

**Interfaces:**
- Produces: os 3 arquivos JSON com o formato exato consumido pelos modelos da Task 2. Contagem esperada: 15 linhas, 166 estações, 30 horários de linha (2 por linha).

- [ ] **Step 1: Escrever o teste (failing)**

`test/data/static_data_test.dart`:

```dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/models/line.dart';
import 'package:seu_metro/models/line_schedule.dart';
import 'package:seu_metro/models/station.dart';

Map<String, dynamic> _load(String name) {
  final raw = File('assets/data/$name').readAsStringSync();
  return jsonDecode(raw) as Map<String, dynamic>;
}

void main() {
  test('lines.json tem 15 linhas e ids únicos', () {
    final lines = (_load('lines.json')['lines'] as List)
        .map((e) => Line.fromJson(e as Map<String, dynamic>))
        .toList();
    expect(lines.length, 15);
    expect(lines.map((l) => l.id).toSet().length, 15);
  });

  test('stations.json tem 166 estações com ids únicos', () {
    final stations = (_load('stations.json')['stations'] as List)
        .map((e) => Station.fromJson(e as Map<String, dynamic>))
        .toList();
    expect(stations.length, 166);
    expect(stations.map((s) => s.id).toSet().length, 166);
  });

  test('stations.json: cada lineId referenciada existe em lines.json', () {
    final lineIds = (_load('lines.json')['lines'] as List)
        .map((e) => (e as Map<String, dynamic>)['id'] as String)
        .toSet();
    final stations = (_load('stations.json')['stations'] as List)
        .map((e) => Station.fromJson(e as Map<String, dynamic>))
        .toList();
    for (final s in stations) {
      for (final l in s.lineIds) {
        expect(lineIds.contains(l), isTrue,
            reason: 'estação ${s.name} referencia linha inexistente $l');
      }
    }
  });

  test('stations.json: cada estação tem ao menos uma linha', () {
    final stations = (_load('stations.json')['stations'] as List)
        .map((e) => Station.fromJson(e as Map<String, dynamic>))
        .toList();
    for (final s in stations) {
      expect(s.lineIds.isNotEmpty, isTrue, reason: '${s.name} sem linha');
    }
  });

  test('lines.json: stationIds existem e batem com lines das estações', () {
    final rawLines = _load('lines.json')['lines'] as List;
    final rawStations = _load('stations.json')['stations'] as List;
    final lines = rawLines.map((e) => Line.fromJson(e as Map<String, dynamic>)).toList();
    final stations = rawStations.map((e) => Station.fromJson(e as Map<String, dynamic>)).toList();
    final byId = {for (final s in stations) s.id: s};
    for (final line in lines) {
      for (final sid in line.stationIds) {
        final station = byId[sid];
        expect(station, isNotNull, reason: 'linha ${line.id} referencia estação inexistente $sid');
        expect(station!.lineIds, contains(line.id),
            reason: 'estação $sid não marca a linha ${line.id}');
      }
    }
  });

  test('schedules.json: 2 horários por linha e campos válidos', () {
    final scheds = (_load('schedules.json')['schedules'] as List)
        .map((e) => LineSchedule.fromJson(e as Map<String, dynamic>))
        .toList();
    expect(scheds.length, 30);
    expect(scheds.where((s) => s.lineId == '1').length, 2);
    for (final s in scheds) {
      expect(s.firstTrain, matches(RegExp(r'^\d{2}:\d{2}$')));
      expect(s.lastTrain, matches(RegExp(r'^\d{2}:\d{2}$')));
    }
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/data/static_data_test.dart`
Expected: FAIL (arquivos não existem / contagens erradas).

- [ ] **Step 3: Criar `assets/data/lines.json`**

Conteúdo EXATO (15 linhas; cores oficiais; operadores `metro` | `cptm`; `stations` é a lista ordenada de ids de estação em ordem de viagem — é a fonte da topologia do grafo):

```json
{
  "lines": [
    { "id": "1",  "name": "Linha 1 - Azul",      "colorHex": "#00378c", "operator": "metro", "terminalA": "Jabaquara", "terminalB": "Tucuruvi", "stations": ["tucuruvi","parada_inglesa","jardim_sao_paulo_ayrton_senna","santana","carandiru","portuguesa_tiete","armenia","tiradentes","luz","sao_bento","se","liberdade","sao_joaquim","vergueiro","paraiso","ana_rosa","vila_mariana","santa_cruz","praca_da_arvore","saude","sao_judas","conceicao","jabaquara"] },
    { "id": "2",  "name": "Linha 2 - Verde",     "colorHex": "#186d55", "operator": "metro", "terminalA": "Vila Madalena", "terminalB": "Vila Prudente", "stations": ["vila_madalena","sumare","clinicas","consolacao","trianon_masp","brigadeiro","paraiso","ana_rosa","chacara_klabin","santos_imigrantes","alto_do_ipiranga","sacoma","tamanduatei","vila_prudente"] },
    { "id": "3",  "name": "Linha 3 - Vermelha",  "colorHex": "#f51200", "operator": "metro", "terminalA": "Palmeiras-Barra Funda", "terminalB": "Corinthians-Itaquera", "stations": ["palmeiras_barra_funda","marechal_deodoro","santa_cecilia","republica","anhangabau","sao_bento","se","pedro_ii","bras","bresser_mooca","belem","tatuape","carrao","penha","vila_matilde","guilhermina_esperanca","patriarca_vila_re","artur_alvim","corinthians_itaquera"] },
    { "id": "4",  "name": "Linha 4 - Amarela",   "colorHex": "#efba00", "operator": "metro", "terminalA": "Luz", "terminalB": "Vila Sônia-Professora Elisabeth Tenreiro", "stations": ["luz","republica","higienopolis_mackenzie","paulista_pernambucanas","oscar_freire","fradique_coutinho","faria_lima","pinheiros","butanta","sao_paulo_morumbi","vila_sonia"] },
    { "id": "5",  "name": "Linha 5 - Lilás",     "colorHex": "#9271b1", "operator": "metro", "terminalA": "Capão Redondo", "terminalB": "Chácara Klabin", "stations": ["capao_redondo","campo_limpo","vila_das_belezas","giovanni_gronchi","santo_amaro","largo_treze","adolfo_pinheiro","alto_da_boa_vista","borba_gato","brooklin","campo_belo","eucaliptos","moema","aacd_servidor","hospital_sao_paulo","santa_cruz","chacara_klabin"] },
    { "id": "6",  "name": "Linha 6 - Laranja",   "colorHex": "#f27c00", "operator": "metro", "terminalA": "Brasilândia", "terminalB": "São Joaquim", "stations": [] },
    { "id": "7",  "name": "Linha 7 - Rubi",      "colorHex": "#c80857", "operator": "cptm", "terminalA": "Luz", "terminalB": "Jundiaí", "stations": ["luz","palmeiras_barra_funda","agua_branca","lapa","piqueri","pirituba","vila_clarice","jaragua","perus","caieiras","franco_da_rocha","baltazar_fidelis","francisco_morato","botujuru","campo_limpo_paulista","varzea_paulista","jundiai"] },
    { "id": "8",  "name": "Linha 8 - Diamante",  "colorHex": "#949488", "operator": "cptm", "terminalA": "Júlio Prestes", "terminalB": "Amador Bueno", "stations": ["julio_prestes","palmeiras_barra_funda","lapa","domingos_de_moraes","imperatriz_leopoldina","presidente_altino","osasco","comandante_sampaio","quitauna","general_miguel_costa","carapicuiba","santa_terezinha","antonio_joao","barueri","jardim_belval","jardim_silveira","jandira","sagrado_coracao","engenheiro_cardoso","itapevi","santa_rita","amador_bueno"] },
    { "id": "9",  "name": "Linha 9 - Esmeralda", "colorHex": "#00aa80", "operator": "cptm", "terminalA": "Osasco", "terminalB": "Varginha", "stations": ["osasco","presidente_altino","cidade_universitaria","villa_lobos_jaguare","cidade_jardim","hebraica_reboucas","pinheiros","morumbi","socorro","santo_amaro","jurubatuba","autodromo","primavera_interlagos","grajau","varginha"] },
    { "id": "10", "name": "Linha 10 - Turquesa", "colorHex": "#00998c", "operator": "cptm", "terminalA": "Brás", "terminalB": "Rio Grande da Serra", "stations": ["bras","juventus_mooca","ipiranga","tamanduatei","sao_caetano_do_sul","utinga","prefeito_saladino","santo_andre","capuava","maua","guapituba","ribeirao_pires","rio_grande_da_serra"] },
    { "id": "11", "name": "Linha 11 - Coral",    "colorHex": "#e33825", "operator": "cptm", "terminalA": "Luz", "terminalB": "Estudantes", "stations": ["luz","bras","tatuape","corinthians_itaquera","dom_bosco","jose_bonifacio","guaianases","antonio_gianneti","ferraz_de_vasconcelos","poa","calmon_viana","suzano","jundiapeba","braz_cubas","mogi_das_cruzes","estudantes"] },
    { "id": "12", "name": "Linha 12 - Safira",   "colorHex": "#193e81", "operator": "cptm", "terminalA": "Brás", "terminalB": "Calmon Viana", "stations": ["bras","tatuape","engenheiro_goulart","usp_leste","comendador_ermelino","sao_miguel_paulista","jardim_helena_vila_mara","itaim_paulista","jardim_romano","engenheiro_manoel_feio","itaquaquecetuba","aracare","calmon_viana"] },
    { "id": "13", "name": "Linha 13 - Jade",     "colorHex": "#2d9f7e", "operator": "cptm", "terminalA": "Engenheiro Goulart", "terminalB": "Aeroporto-Guarulhos", "stations": ["engenheiro_goulart","guarulhos_cecap","aeroporto_guarulhos"] },
    { "id": "15", "name": "Linha 15 - Prata",    "colorHex": "#9d968d", "operator": "metro", "terminalA": "Vila Prudente", "terminalB": "São Mateus", "stations": ["vila_prudente","oratorio","sao_lucas","camilo_haddad","vila_tolstoi","vila_uniao","jardim_planalto","sapopemba","fazenda_da_japiim","sao_mateus"] },
    { "id": "17", "name": "Linha 17 - Ouro",     "colorHex": "#d0a238", "operator": "cptm", "terminalA": "São Paulo-Morumbi", "terminalB": "Congonhas", "stations": [] }
  ]
}
```

- [ ] **Step 4: Criar `assets/data/stations.json`**

Estrutura: `{ "stations": [ {id, name, lat, lon, lines:[...]}, ... ] }`. `id` = slug do nome (sem acento/espaço); `lines` lista as linhas servidas. A lista é **plana** — cada estação única aparece UMA vez (não importa a ordem do array; a topologia vem de `lines.json`). Contagem: **166 estações**. Os agrupamentos abaixo são apenas organizacionais; ao gerar o JSON, dedupe por `id`.

Linha 1-Azul (N→S, 23):
`tucuruvi`/-23.466,-46.601/[1], `parada_inglesa`/-23.479,-46.604/[1], `jardim_sao_paulo_ayrton_senna`/-23.491,-46.606/[1], `santana`/-23.504,-46.613/[1], `carandiru`/-23.516,-46.621/[1], `portuguesa_tiete`/-23.528,-46.625/[1], `armenia`/-23.531,-46.631/[1], `tiradentes`/-23.534,-46.634/[1], `luz`/-23.534,-46.635/[1,3,4,7,11], `sao_bento`/-23.543,-46.634/[1,3], `se`/-23.550,-46.633/[1,2,3], `liberdade`/-23.558,-46.636/[1], `sao_joaquim`/-23.565,-46.641/[1], `vergueiro`/-23.573,-46.638/[1], `paraiso`/-23.574,-46.642/[1,2], `ana_rosa`/-23.578,-46.646/[1,2], `vila_mariana`/-23.589,-46.644/[1], `santa_cruz`/-23.598,-46.640/[1,5], `praca_da_arvore`/-23.608,-46.638/[1], `saude`/-23.619,-46.639/[1], `sao_judas`/-23.628,-46.641/[1], `conceicao`/-23.636,-46.642/[1], `jabaquara`/-23.646,-46.639/[1]

Linha 2-Verde (W→E, 14):
`vila_madalena`/-23.553,-46.690/[2], `sumare`/-23.559,-46.678/[2], `clinicas`/-23.555,-46.669/[2], `consolacao`/-23.557,-46.658/[2], `trianon_masp`/-23.560,-46.653/[2], `brigadeiro`/-23.568,-46.652/[2], `paraiso`/-23.574,-46.642/[1,2], `ana_rosa`/-23.578,-46.646/[1,2], `chacara_klabin`/-23.577,-46.622/[2,5], `santos_imigrantes`/-23.597,-46.613/[2], `alto_do_ipiranga`/-23.601,-46.596/[2], `sacoma`/-23.606,-46.581/[2], `tamanduatei`/-23.602,-46.570/[2,10], `vila_prudente`/-23.582,-46.566/[2,15]

Linha 3-Vermelha (W→E, 18):
`palmeiras_barra_funda`/-23.524,-46.668/[3,7,8], `marechal_deodoro`/-23.529,-46.654/[3], `santa_cecilia`/-23.536,-46.648/[3], `republica`/-23.543,-46.643/[3,4], `anhangabau`/-23.545,-46.637/[3], `se`/-23.550,-46.633/[1,2,3], `pedro_ii`/-23.550,-46.629/[3], `bras`/-23.545,-46.615/[3,7,10,11,12], `bresser_mooca`/-23.548,-46.604/[3], `belem`/-23.550,-46.596/[3], `tatuape`/-23.542,-46.577/[3,11,12], `carrao`/-23.545,-46.565/[3], `penha`/-23.542,-46.555/[3], `vila_matilde`/-23.539,-46.547/[3], `guilhermina_esperanca`/-23.536,-46.538/[3], `patriarca_vila_re`/-23.532,-46.528/[3], `artur_alvim`/-23.535,-46.520/[3], `corinthians_itaquera`/-23.541,-46.509/[3,11]

Linha 4-Amarela (Luz→Vila Sônia, 11):
`luz`/-23.534,-46.635/[1,3,4,7,11], `republica`/-23.543,-46.643/[3,4], `higienopolis_mackenzie`/-23.550,-46.651/[4], `paulista_pernambucanas`/-23.557,-46.652/[4], `oscar_freire`/-23.565,-46.670/[4], `fradique_coutinho`/-23.572,-46.687/[4], `faria_lima`/-23.578,-46.697/[4], `pinheiros`/-23.567,-46.700/[4,9], `butanta`/-23.570,-46.715/[4], `sao_paulo_morumbi`/-23.598,-46.733/[4], `vila_sonia`/-23.610,-46.740/[4]

Linha 5-Lilás (SW→NE, 17):
`capao_redondo`/-23.650,-46.764/[5], `campo_limpo`/-23.643,-46.754/[5], `vila_das_belezas`/-23.634,-46.745/[5], `giovanni_gronchi`/-23.627,-46.737/[5], `santo_amaro`/-23.651,-46.710/[5,9], `largo_treze`/-23.652,-46.703/[5], `adolfo_pinheiro`/-23.645,-46.694/[5], `alto_da_boa_vista`/-23.636,-46.688/[5], `borba_gato`/-23.625,-46.684/[5], `brooklin`/-23.617,-46.679/[5], `campo_belo`/-23.622,-46.671/[5], `eucaliptos`/-23.620,-46.662/[5], `moema`/-23.610,-46.658/[5], `aacd_servidor`/-23.604,-46.652/[5], `hospital_sao_paulo`/-23.601,-46.647/[5], `santa_cruz`/-23.598,-46.640/[1,5], `chacara_klabin`/-23.577,-46.622/[2,5]

Linha 7-Rubi (S→N, 16):
`luz`/-23.534,-46.635/[1,3,4,7,11], `palmeiras_barra_funda`/-23.524,-46.668/[3,7,8], `agua_branca`/-23.523,-46.678/[7], `lapa`/-23.522,-46.702/[7,8], `piqueri`/-23.526,-46.716/[7], `pirituba`/-23.502,-46.730/[7], `vila_clarice`/-23.491,-46.728/[7], `jaragua`/-23.480,-46.734/[7], `perus`/-23.404,-46.751/[7], `caieiras`/-23.364,-46.742/[7], `franco_da_rocha`/-23.322,-46.726/[7], `baltazar_fidelis`/-23.294,-46.716/[7], `francisco_morato`/-23.281,-46.744/[7], `botujuru`/-23.246,-46.735/[7], `campo_limpo_paulista`/-23.208,-46.772/[7], `varzea_paulista`/-23.209,-46.823/[7], `jundiai`/-23.196,-46.883/[7]

Linha 8-Diamante (SW, 21):
`julio_prestes`/-23.533,-46.642/[8], `palmeiras_barra_funda`/-23.524,-46.668/[3,7,8], `lapa`/-23.522,-46.702/[7,8], `domingos_de_moraes`/-23.523,-46.712/[8], `imperatriz_leopoldina`/-23.525,-46.723/[8], `presidente_altino`/-23.531,-46.735/[8,9], `osasco`/-23.532,-46.791/[8,9], `comandante_sampaio`/-23.535,-46.799/[8], `quitauna`/-23.537,-46.808/[8], `general_miguel_costa`/-23.543,-46.817/[8], `carapicuiba`/-23.523,-46.835/[8], `santa_terezinha`/-23.520,-46.846/[8], `antonio_joao`/-23.521,-46.858/[8], `barueri`/-23.511,-46.874/[8], `jardim_belval`/-23.515,-46.887/[8], `jardim_silveira`/-23.514,-46.900/[8], `jandira`/-23.529,-46.905/[8], `sagrado_coracao`/-23.538,-46.913/[8], `engenheiro_cardoso`/-23.541,-46.923/[8], `itapevi`/-23.549,-46.934/[8], `santa_rita`/-23.560,-46.958/[8], `amador_bueno`/-23.573,-46.988/[8]

Linha 9-Esmeralda (N→S, 15):
`osasco`/-23.532,-46.791/[8,9], `presidente_altino`/-23.531,-46.735/[8,9], `cidade_universitaria`/-23.552,-46.721/[9], `villa_lobos_jaguare`/-23.552,-46.708/[9], `cidade_jardim`/-23.558,-46.697/[9], `hebraica_reboucas`/-23.563,-46.692/[9], `pinheiros`/-23.567,-46.700/[4,9], `morumbi`/-23.601,-46.719/[9], `socorro`/-23.627,-46.714/[9], `santo_amaro`/-23.651,-46.710/[5,9], `jurubatuba`/-23.665,-46.690/[9], `autodromo`/-23.702,-46.695/[9], `primavera_interlagos`/-23.696,-46.667/[9], `grajau`/-23.714,-46.684/[9], `varginha`/-23.740,-46.696/[9]

Linha 10-Turquesa (SE, 13):
`bras`/-23.545,-46.615/[3,7,10,11,12], `juventus_mooca`/-23.547,-46.606/[10], `ipiranga`/-23.578,-46.600/[10], `tamanduatei`/-23.602,-46.570/[2,10], `sao_caetano_do_sul`/-23.621,-46.573/[10], `utinga`/-23.632,-46.568/[10], `prefeito_saladino`/-23.643,-46.563/[10], `santo_andre`/-23.663,-46.538/[10], `capuava`/-23.692,-46.496/[10], `maua`/-23.668,-46.461/[10], `guapituba`/-23.699,-46.414/[10], `ribeirao_pires`/-23.711,-46.413/[10], `rio_grande_da_serra`/-23.744,-46.402/[10]

Linha 11-Coral (E, 16):
`luz`/-23.534,-46.635/[1,3,4,7,11], `bras`/-23.545,-46.615/[3,7,10,11,12], `tatuape`/-23.542,-46.577/[3,11,12], `corinthians_itaquera`/-23.541,-46.509/[3,11], `dom_bosco`/-23.542,-46.493/[11], `jose_bonifacio`/-23.540,-46.474/[11], `guaianases`/-23.540,-46.454/[11], `antonio_gianneti`/-23.542,-46.438/[11], `ferraz_de_vasconcelos`/-23.541,-46.368/[11], `poa`/-23.528,-46.344/[11], `calmon_viana`/-23.536,-46.299/[11,12], `suzano`/-23.542,-46.312/[11], `jundiapeba`/-23.552,-46.263/[11], `braz_cubas`/-23.568,-46.231/[11], `mogi_das_cruzes`/-23.524,-46.191/[11], `estudantes`/-23.546,-46.179/[11]

Linha 12-Safira (NE, 13):
`bras`/-23.545,-46.615/[3,7,10,11,12], `tatuape`/-23.542,-46.577/[3,11,12], `engenheiro_goulart`/-23.528,-46.536/[12,13], `usp_leste`/-23.520,-46.514/[12], `comendador_ermelino`/-23.512,-46.491/[12], `sao_miguel_paulista`/-23.497,-46.454/[12], `jardim_helena_vila_mara`/-23.493,-46.424/[12], `itaim_paulista`/-23.497,-46.398/[12], `jardim_romano`/-23.492,-46.375/[12], `engenheiro_manoel_feio`/-23.493,-46.348/[12], `itaquaquecetuba`/-23.484,-46.349/[12], `aracare`/-23.499,-46.330/[12], `calmon_viana`/-23.536,-46.299/[11,12]

Linha 13-Jade (N, 3):
`engenheiro_goulart`/-23.528,-46.536/[12,13], `guarulhos_cecap`/-23.454,-46.524/[13], `aeroporto_guarulhos`/-23.434,-46.476/[13]

Linha 15-Prata (W→E, 10):
`vila_prudente`/-23.582,-46.566/[2,15], `oratorio`/-23.574,-46.553/[15], `sao_lucas`/-23.563,-46.538/[15], `camilo_haddad`/-23.553,-46.523/[15], `vila_tolstoi`/-23.546,-46.510/[15], `vila_uniao`/-23.538,-46.496/[15], `jardim_planalto`/-23.534,-46.484/[15], `sapopemba`/-23.526,-46.472/[15], `fazenda_da_japiim`/-23.519,-46.460/[15], `sao_mateus`/-23.510,-46.446/[15]

**Notas de decisão**: linhas 6 e 17 NÃO têm estações no v1 (operação parcial/em implantação; status ainda aparece via API), logo `stations` é `[]` nelas. Estações compartilhadas entre linhas têm um ÚNICO id e `lines` com todas as linhas. Escreva o arquivo como um array JSON plano, com uma entrada por id único e os campos `id, name, lat, lon, lines`.

- [ ] **Step 5: Criar `assets/data/schedules.json`**

`{ "schedules": [ {lineId, direction, terminal, firstTrain, lastTrain, headwayPeakMin, headwayNormalMin}, ... ] }`. Direção `A` = terminalA, `B` = terminalB. 30 entradas (2 por linha, inclusive 6 e 17). Valores tabelados oficiais aproximados (v1):

```json
{
  "schedules": [
    { "lineId": "1",  "direction": "A", "terminal": "Jabaquara", "firstTrain": "04:40", "lastTrain": "00:29", "headwayPeakMin": 2,  "headwayNormalMin": 6 },
    { "lineId": "1",  "direction": "B", "terminal": "Tucuruvi", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 2,  "headwayNormalMin": 6 },
    { "lineId": "2",  "direction": "A", "terminal": "Vila Madalena", "firstTrain": "04:40", "lastTrain": "00:29", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "2",  "direction": "B", "terminal": "Vila Prudente", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "3",  "direction": "A", "terminal": "Palmeiras-Barra Funda", "firstTrain": "04:40", "lastTrain": "00:30", "headwayPeakMin": 2,  "headwayNormalMin": 6 },
    { "lineId": "3",  "direction": "B", "terminal": "Corinthians-Itaquera", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 2,  "headwayNormalMin": 6 },
    { "lineId": "4",  "direction": "A", "terminal": "Luz", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "4",  "direction": "B", "terminal": "Vila Sônia", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "5",  "direction": "A", "terminal": "Capão Redondo", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "5",  "direction": "B", "terminal": "Chácara Klabin", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 3,  "headwayNormalMin": 7 },
    { "lineId": "6",  "direction": "A", "terminal": "Brasilândia", "firstTrain": "05:00", "lastTrain": "23:00", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "6",  "direction": "B", "terminal": "São Joaquim", "firstTrain": "05:00", "lastTrain": "23:00", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "7",  "direction": "A", "terminal": "Luz", "firstTrain": "04:00", "lastTrain": "00:18", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "7",  "direction": "B", "terminal": "Jundiaí", "firstTrain": "04:00", "lastTrain": "23:47", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "8",  "direction": "A", "terminal": "Júlio Prestes", "firstTrain": "04:00", "lastTrain": "00:24", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "8",  "direction": "B", "terminal": "Amador Bueno", "firstTrain": "04:00", "lastTrain": "23:44", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "9",  "direction": "A", "terminal": "Osasco", "firstTrain": "04:00", "lastTrain": "00:18", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "9",  "direction": "B", "terminal": "Varginha", "firstTrain": "04:00", "lastTrain": "23:58", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "10", "direction": "A", "terminal": "Brás", "firstTrain": "04:00", "lastTrain": "00:18", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "10", "direction": "B", "terminal": "Rio Grande da Serra", "firstTrain": "04:00", "lastTrain": "23:31", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "11", "direction": "A", "terminal": "Luz", "firstTrain": "04:00", "lastTrain": "00:20", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "11", "direction": "B", "terminal": "Estudantes", "firstTrain": "04:00", "lastTrain": "23:39", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "12", "direction": "A", "terminal": "Brás", "firstTrain": "04:00", "lastTrain": "00:20", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "12", "direction": "B", "terminal": "Calmon Viana", "firstTrain": "04:00", "lastTrain": "23:44", "headwayPeakMin": 6,  "headwayNormalMin": 12 },
    { "lineId": "13", "direction": "A", "terminal": "Engenheiro Goulart", "firstTrain": "04:00", "lastTrain": "00:14", "headwayPeakMin": 12, "headwayNormalMin": 20 },
    { "lineId": "13", "direction": "B", "terminal": "Aeroporto-Guarulhos", "firstTrain": "04:00", "lastTrain": "00:14", "headwayPeakMin": 12, "headwayNormalMin": 20 },
    { "lineId": "15", "direction": "A", "terminal": "Vila Prudente", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 4,  "headwayNormalMin": 8 },
    { "lineId": "15", "direction": "B", "terminal": "São Mateus", "firstTrain": "04:40", "lastTrain": "00:00", "headwayPeakMin": 4,  "headwayNormalMin": 8 },
    { "lineId": "17", "direction": "A", "terminal": "São Paulo-Morumbi", "firstTrain": "05:00", "lastTrain": "23:00", "headwayPeakMin": 5,  "headwayNormalMin": 10 },
    { "lineId": "17", "direction": "B", "terminal": "Congonhas", "firstTrain": "05:00", "lastTrain": "23:00", "headwayPeakMin": 5,  "headwayNormalMin": 10 }
  ]
}
```

- [ ] **Step 6: Rodar o teste para passar**

Run: `flutter test test/data/static_data_test.dart`
Expected: PASS (ajuste qualquer contagem se o dado final divergir do esperado — o contrato é o schema e a consistência, não a contagem exata; se ajustar, atualize o teste para o valor real).

- [ ] **Step 7: Analisar e commitar**

Run: `flutter analyze` → limpo.
```powershell
git add -A
git commit -m "feat: dados estáticos da malha Metrô SP + CPTM"
```

---

### Task 4: Repositórios de dados (lines, stations, schedules)

**Files:**
- Create: `lib/data/repositories/line_repository.dart`, `lib/data/repositories/station_repository.dart`, `lib/data/repositories/schedule_repository.dart`
- Test: `test/data/repositories_test.dart`

**Interfaces:**
- Consumes: modelos da Task 2; JSONs da Task 3.
- Produces:
  - `class LineRepository { Future<List<Line>> getAll(); Future<Map<String, Line>> byId(); }`
  - `class StationRepository { Future<List<Station>> getAll(); Future<Map<String, Station>> byId(); }`
  - `class ScheduleRepository { Future<List<LineSchedule>> getAll(); }`
  - Construtor de cada um recebe `Future<String> Function(String assetPath)` (loader injetável; default `rootBundle.loadString`).
  - Também: `class AssetLoader { Future<String> load(String path) => rootBundle.loadString(path); }`

- [ ] **Step 1: Escrever o teste (failing)**

`test/data/repositories_test.dart`:

```dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/data/repositories/line_repository.dart';
import 'package:seu_metro/data/repositories/schedule_repository.dart';
import 'package:seu_metro/data/repositories/station_repository.dart';

Future<String> _diskLoader(String path) async => File(path).readAsStringSync();

void main() {
  test('LineRepository carrega 15 linhas', () async {
    final repo = LineRepository(load: _diskLoader);
    final lines = await repo.getAll();
    expect(lines.length, 15);
    expect(lines.any((l) => l.id == '1'), isTrue);
  });

  test('StationRepository carrega 166 estações e byId funciona', () async {
    final repo = StationRepository(load: _diskLoader);
    final stations = await repo.getAll();
    expect(stations.length, 166);
    final byId = await repo.byId();
    expect(byId['paraiso']!.lineIds, contains('1'));
  });

  test('ScheduleRepository carrega 30 horários', () async {
    final repo = ScheduleRepository(load: _diskLoader);
    final scheds = await repo.getAll();
    expect(scheds.length, 30);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/data/repositories_test.dart`
Expected: FAIL (classes não existem).

- [ ] **Step 3: Implementar os repositórios**

`lib/data/repositories/line_repository.dart`:

```dart
import 'dart:convert';

import 'package:flutter/services.dart';
import 'package:seu_metro/models/line.dart';

typedef AssetLoader = Future<String> Function(String assetPath);

Future<String> defaultAssetLoader(String assetPath) =>
    rootBundle.loadString(assetPath);

class LineRepository {
  final AssetLoader load;
  LineRepository({AssetLoader? load}) : load = load ?? defaultAssetLoader;

  Future<List<Line>> getAll() async {
    final raw = await load('assets/data/lines.json');
    final data = jsonDecode(raw) as Map<String, dynamic>;
    return (data['lines'] as List)
        .map((e) => Line.fromJson(e as Map<String, dynamic>))
        .toList();
  }

  Future<Map<String, Line>> byId() async {
    final all = await getAll();
    return {for (final l in all) l.id: l};
  }
}
```

`lib/data/repositories/station_repository.dart` (mesmo padrão, campo `stations`, `Station.fromJson`).

`lib/data/repositories/schedule_repository.dart` (mesmo padrão, campo `schedules`, `LineSchedule.fromJson`).

- [ ] **Step 4: Rodar o teste para passar**

Run: `flutter test test/data/repositories_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: repositórios de linhas, estações e horários"
```

---

### Task 5: Pathfinding (grafo + A* com penalidade de baldeação)

**Files:**
- Create: `lib/services/pathfinding/metro_graph.dart`
- Test: `test/services/metro_graph_test.dart`

**Interfaces:**
- Consumes: `Station`/`Line` (Task 2).
- Produces: `class MetroGraph { MetroGraph.build(List<Line> lines, List<Station> stations); RoutePlan? plan(String fromId, String toId); }`. Peso da aresta: 2 minutos por estação vizinha na mesma linha; penalidade de baldeação: 3 minutos ao trocar de linha na mesma estação.

- [ ] **Step 1: Escrever o teste (failing)**

`test/services/metro_graph_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/models/line.dart';
import 'package:seu_metro/models/station.dart';
import 'package:seu_metro/services/pathfinding/metro_graph.dart';

Line _line(String id, List<String> stationIds) => Line(
      id: id, name: 'Linha $id', colorValue: 0xFF000000,
      operator: 'metro', terminalA: 'A', terminalB: 'B',
      stationIds: stationIds,
    );

Station _st(String id, List<String> lines) =>
    Station(id: id, name: id, lat: 0, lon: 0, lineIds: lines);

void main() {
  // Rota direta na linha 1: tucuruvi -> liberdade (mesma linha, sem baldeação)
  final line1 = _line('1', ['tucuruvi', 'santana', 'luz', 'se', 'liberdade']);
  final line2 = _line('2', ['luz', 'paraiso']);
  final stations = [
    _st('tucuruvi', ['1']),
    _st('santana', ['1']),
    _st('luz', ['1', '2']),
    _st('se', ['1']),
    _st('liberdade', ['1']),
    _st('paraiso', ['2']),
  ];

  test('rota direta sem baldeação gera uma única perna', () {
    final graph = MetroGraph.build([line1, line2], stations);
    final plan = graph.plan('tucuruvi', 'liberdade')!;
    expect(plan.legs.length, 1);
    expect(plan.legs.first.fromStationId, 'tucuruvi');
    expect(plan.legs.first.toStationId, 'liberdade');
    expect(plan.totalMinutes, 4 * 2); // 4 estações vizinhas percorridas
  });

  test('baldeação gera duas pernas e penaliza troca', () {
    final graph = MetroGraph.build([line1, line2], stations);
    final plan = graph.plan('tucuruvi', 'paraiso')!;
    expect(plan.legs.length, 2);
    expect(plan.legs.last.lineId, '2');
    expect(plan.transferStationNames, ['luz']);
    // 2 arestas na linha 1 (tucuruvi->santana->luz) + 3 de baldeação + 1 aresta na linha 2
    expect(plan.totalMinutes, 2 * 2 + 3 + 1 * 2);
  });

  test('origem igual ao destino retorna plano vazio', () {
    final graph = MetroGraph.build([line1, line2], stations);
    final plan = graph.plan('tucuruvi', 'tucuruvi')!;
    expect(plan.legs, isEmpty);
    expect(plan.totalMinutes, 0);
  });

  test('sem caminho retorna null', () {
    final isolated = _st('sozinho', ['9']);
    final graph = MetroGraph.build([line1, line2], stations + [isolated]);
    expect(graph.plan('sozinho', 'tucuruvi'), isNull);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/services/metro_graph_test.dart`
Expected: FAIL (MetroGraph não existe).

- [ ] **Step 3: Implementar `lib/services/pathfinding/metro_graph.dart`**

```dart
import 'dart:collection';

import 'package:seu_metro/models/line.dart';
import 'package:seu_metro/models/route.dart';
import 'package:seu_metro/models/station.dart';

class _Edge {
  final String toNode;
  final int cost;
  final bool isTransfer;
  final String lineId;
  _Edge(this.toNode, this.cost, this.isTransfer, this.lineId);
}

class MetroGraph {
  static const transferPenalty = 3;
  static const minutePerStation = 2;

  // Nó = "estação|linha" (ex.: "luz|1"). Baldeação liga nós da mesma estação
  // com aresta de penalidade. Isso torna a reconstrução de pernas trivial.
  final Map<String, List<_Edge>> _adjacency = {};
  final Map<String, Station> _stations;
  final Map<String, Line> _lines;

  MetroGraph.build(List<Line> lines, List<Station> stations)
      : _stations = {for (final s in stations) s.id: s},
        _lines = {for (final l in lines) l.id: l} {
    String nodeId(String stationId, String lineId) => '$stationId|$lineId';

    for (final station in stations) {
      for (final lineId in station.lineIds) {
        _adjacency.putIfAbsent(nodeId(station.id, lineId), () => []);
      }
    }

    for (final line in lines) {
      final ordered = line.stationIds;
      for (var i = 0; i < ordered.length - 1; i++) {
        final a = nodeId(ordered[i], line.id);
        final b = nodeId(ordered[i + 1], line.id);
        _adjacency[a]!.add(_Edge(b, minutePerStation, false, line.id));
        _adjacency[b]!.add(_Edge(a, minutePerStation, false, line.id));
      }
    }

    for (final station in stations) {
      final lines = station.lineIds;
      for (var i = 0; i < lines.length; i++) {
        for (var j = i + 1; j < lines.length; j++) {
          final a = nodeId(station.id, lines[i]);
          final b = nodeId(station.id, lines[j]);
          _adjacency[a]!.add(_Edge(b, transferPenalty, true, lines[j]));
          _adjacency[b]!.add(_Edge(a, transferPenalty, true, lines[i]));
        }
      }
    }
  }

  RoutePlan? plan(String fromId, String toId) {
    if (fromId == toId) {
      return const RoutePlan(legs: [], totalMinutes: 0, transferStationNames: []);
    }
    final fromStation = _stations[fromId];
    final toStation = _stations[toId];
    if (fromStation == null || toStation == null) return null;

    final starts = fromStation.lineIds.map((l) => '$fromId|$l').toList();
    final ends = toStation.lineIds.map((l) => '$toId|$l').toSet();

    final dist = <String, int>{};
    final prev = <String, String>{}; // nó → nó pai
    final edgeOf = <String, _Edge>{}; // nó → aresta que levou até ele
    final pq = SplayTreeMap<String, int>();
    for (final s in starts) {
      dist[s] = 0;
      pq[s] = 0;
    }
    String? goal;
    while (pq.isNotEmpty) {
      final current = pq.firstKey()!;
      final d = pq.remove(current)!;
      if (d != dist[current]) continue;
      if (ends.contains(current)) {
        goal = current;
        break;
      }
      for (final e in _adjacency[current] ?? const <_Edge>[]) {
        final nd = d + e.cost;
        if (nd < (dist[e.toNode] ?? 1 << 30)) {
          dist[e.toNode] = nd;
          prev[e.toNode] = current;
          edgeOf[e.toNode] = e;
          pq[e.toNode] = nd;
        }
      }
    }
    if (goal == null) return null;
    return _reconstruct(goal!, prev, edgeOf, dist[goal]!);
  }

  RoutePlan _reconstruct(String goal, Map<String, String> prev,
      Map<String, _Edge> edgeOf, int total) {
    final nodeIds = <String>[];
    var node = goal;
    while (true) {
      nodeIds.add(node);
      final parent = prev[node];
      if (parent == null) break;
      node = parent;
    }
    nodeIds.reverse();
    // nodeIds[0] é o nó inicial (sem aresta) e nodeIds[1..] têm aresta em edgeOf

    final legs = <RouteLeg>[];
    final transfers = <String>{};
    final k = nodeIds.length - 1;
    String curLine = edgeOf[nodeIds[1]]!.lineId;
    int legStart = 0;
    for (var i = 1; i <= k; i++) {
      final e = edgeOf[nodeIds[i]]!;
      if (e.isTransfer) {
        _closeLeg(legs, nodeIds, legStart, i, curLine);
        transfers.add(_stations[nodeIds[i].split('|').first]!.name);
        legStart = i;
        curLine = e.lineId;
      } else if (e.lineId != curLine) {
        _closeLeg(legs, nodeIds, legStart, i, curLine);
        transfers.add(_stations[nodeIds[i - 1].split('|').first]!.name);
        legStart = i - 1;
        curLine = e.lineId;
      }
    }
    if (legStart <= k) {
      _closeLeg(legs, nodeIds, legStart, k + 1, curLine);
    }
    return RoutePlan(
      legs: legs,
      totalMinutes: total,
      transferStationNames: transfers.toList(),
    );
  }

  void _closeLeg(List<RouteLeg> legs, List<String> nodeIds, int start, int end, String lineId) {
    if (end <= start) return;
    final line = _lines[lineId]!;
    final from = _stations[nodeIds[start].split('|').first]!;
    final to = _stations[nodeIds[end - 1].split('|').first]!;
    final direction = line.terminalA == to.name || line.terminalA == from.name
        ? line.terminalB
        : line.terminalA;
    legs.add(RouteLeg(
      lineId: lineId,
      directionTerminal: direction,
      fromStationId: from.id,
      toStationId: to.id,
      stationCount: end - 1 - start,
      minutes: (end - 1 - start) * minutePerStation,
    ));
  }
}
```

**Como funciona**: cada nó é `"estação|linha"`. Percorrer a mesma linha custa 2 min por estação; trocar de linha na mesma estação custa 3 min (`transferPenalty`). O A* começa em todos os nós de origem e termina em qualquer nó de destino. `prev` guarda o nó pai; `edgeOf` guarda a aresta de chegada (com `lineId` e `isTransfer`). A reconstrução caminha `goal → raiz` invertendo a lista; cada aresta de transferência (ou mudança de linha) fecha a perna atual e registra a estação de baldeação.

**Esperado nos testes**: rota direta `tucuruvi→liberdade` = 1 perna, 8 min (4 arestas × 2). Rota `tucuruvi→paraiso` = 2 pernas (linha 1: tucuruvi→santana→luz, 4 min; baldeação em `luz`, 3 min; linha 2: luz→paraiso, 2 min) = 9 min, `transferStationNames == ['luz']`.

- [ ] **Step 4: Rodar os testes até passar**

Run: `flutter test test/services/metro_graph_test.dart`
Expected: PASS (todos os 4 testes).

- [ ] **Step 5: Teste de integração com dados reais**

Adicionar ao mesmo arquivo de teste:

```dart
test('integração: rota luz->santo_amaro passa por baldeação', () async {
  final rawLines = await File('assets/data/lines.json').readAsStringSync();
  final rawStations = await File('assets/data/stations.json').readAsStringSync();
  final lines = (jsonDecode(rawLines)['lines'] as List)
      .map((e) => Line.fromJson(e as Map<String, dynamic>)).toList();
  final stations = (jsonDecode(rawStations)['stations'] as List)
      .map((e) => Station.fromJson(e as Map<String, dynamic>)).toList();
  final graph = MetroGraph.build(lines, stations);
  final plan = graph.plan('luz', 'santo_amaro');
  expect(plan, isNotNull);
  expect(plan!.totalMinutes, greaterThan(0));
});
```
(Nesse arquivo de teste, adicione os imports `dart:convert` e `dart:io` usados acima.)

Run: `flutter test test/services/metro_graph_test.dart`
Expected: PASS

- [ ] **Step 6: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: pathfinding com A* e penalidade de baldeação"
```

---

### Task 6: Estação mais próxima (Haversine)

**Files:**
- Create: `lib/services/location/nearest_station.dart`
- Test: `test/services/nearest_station_test.dart`

**Interfaces:**
- Produces: `double distanceKm(Station a, double lat, double lon);` e `Station? nearestStation(List<Station> stations, double lat, double lon);`

- [ ] **Step 1: Escrever o teste (failing)**

`test/services/nearest_station_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/models/station.dart';
import 'package:seu_metro/services/location/nearest_station.dart';

void main() {
  const stations = [
    Station(id: 'luz', name: 'Luz', lat: -23.534, lon: -46.635, lineIds: ['1']),
    Station(id: 'se', name: 'Sé', lat: -23.550, lon: -46.633, lineIds: ['1']),
  ];

  test('nearestStation retorna a estação mais próxima', () {
    // Perto da Sé
    final result = nearestStation(stations, -23.549, -46.633);
    expect(result!.id, 'se');
  });

  test('distanceKm calcula ~1.8km entre Luz e Sé', () {
    final km = distanceKm(stations[0], stations[1].lat, stations[1].lon);
    expect(km, closeTo(1.8, 0.3));
  });

  test('lista vazia retorna null', () {
    expect(nearestStation(const [], -23.5, -46.6), isNull);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/services/nearest_station_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `lib/services/location/nearest_station.dart`**

```dart
import 'dart:math';

import 'package:seu_metro/models/station.dart';

const _earthRadiusKm = 6371.0;

double distanceKm(Station station, double lat, double lon) {
  const p = pi / 180;
  final dLat = (lat - station.lat) * p;
  final dLon = (lon - station.lon) * p;
  final a = sin(dLat / 2) * sin(dLat / 2) +
      cos(station.lat * p) * cos(lat * p) *
          sin(dLon / 2) * sin(dLon / 2);
  return 2 * _earthRadiusKm * atan2(sqrt(a), sqrt(1 - a));
}

Station? nearestStation(List<Station> stations, double lat, double lon) {
  Station? best;
  var bestKm = double.infinity;
  for (final s in stations) {
    final km = distanceKm(s, lat, lon);
    if (km < bestKm) {
      bestKm = km;
      best = s;
    }
  }
  return best;
}
```

- [ ] **Step 4: Rodar o teste para passar**

Run: `flutter test test/services/nearest_station_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: cálculo de estação mais próxima"
```

---

### Task 7: Status ao vivo — cliente da API + repositório com cache

**Files:**
- Create: `lib/data/status_api/line_status_api_client.dart`, `lib/data/repositories/status_repository.dart`
- Test: `test/data/status_repository_test.dart`, `test/data/line_status_api_client_test.dart`

**Interfaces:**
- Consumes: `LineStatus` (Task 2).
- Produces:
  - `class LineStatusApiClient { final Dio _dio; LineStatusApiClient({Dio? dio}); Future<List<LineStatus>> fetchLines(); }` — GET em `https://apim-proximotrem-prd-brazilsouth-001.azure-api.net/api/v1/lines`, parseia `Data`.
  - `class StatusRepository { StatusRepository({LineStatusApiClient? client}); Future<List<LineStatus>> getStatuses(); }` — cache em memória (`List<LineStatus>? _cache`, `DateTime? _lastFetched`); se cache com <5min retorna cache; senão busca e atualiza cache; em erro retorna cache (se existir) ou re-throw.

- [ ] **Step 1: Escrever os testes (failing)**

`test/data/line_status_api_client_test.dart`:

```dart
import 'package:dio/dio.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/data/status_api/line_status_api_client.dart';

void main() {
  test('fetchLines parseia resposta da API real', () async {
    final dio = Dio(BaseOptions(baseUrl: 'https://apim-proximotrem-prd-brazilsouth-001.azure-api.net/api/v1'));
    final client = LineStatusApiClient(dio: dio);
    final statuses = await client.fetchLines();
    expect(statuses.length, greaterThanOrEqualTo(10));
    expect(statuses.every((s) => s.statusLabel.isNotEmpty), isTrue);
  });

  test('fetchLines falha com erro de rede lança DioException', () async {
    final dio = Dio(BaseOptions(baseUrl: 'http://localhost:1'));
    final client = LineStatusApiClient(dio: dio, timeoutSeconds: 2);
    expect(client.fetchLines(), throwsA(isA<DioException>()));
  });
}
```

`test/data/status_repository_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/data/repositories/status_repository.dart';
import 'package:seu_metro/data/status_api/line_status_api_client.dart';
import 'package:seu_metro/models/line_status.dart';

class _FakeClient implements LineStatusApiClient {
  int calls = 0;
  bool failNext = false;
  @override
  Future<List<LineStatus>> fetchLines() async {
    calls++;
    if (failNext) throw Exception('offline');
    return [LineStatus(lineId: '1', statusCode: 'OperacaoNormal', statusLabel: 'Operação Normal', statusColor: 'verde', description: null, updatedAt: DateTime.now())];
  }
}

void main() {
  test('getStatuses busca na API e faz cache', () async {
    final fake = _FakeClient();
    final repo = StatusRepository(client: fake);
    final a = await repo.getStatuses();
    final b = await repo.getStatuses();
    expect(a.length, 1);
    expect(b.length, 1);
    expect(fake.calls, 1, reason: 'segunda chamada deve usar cache');
  });

  test('erro usa cache antigo', () async {
    final fake = _FakeClient();
    final repo = StatusRepository(client: fake);
    await repo.getStatuses();
    fake.failNext = true;
    final result = await repo.getStatuses();
    expect(result.length, 1);
  });

  test('erro sem cache propaga', () async {
    final fake = _FakeClient()..failNext = true;
    final repo = StatusRepository(client: fake);
    expect(repo.getStatuses(), throwsA(isA<Exception>()));
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/data/line_status_api_client_test.dart test/data/status_repository_test.dart`
Expected: FAIL (classes não existem).

- [ ] **Step 3: Implementar o cliente**

`lib/data/status_api/line_status_api_client.dart`:

```dart
import 'package:dio/dio.dart';
import 'package:seu_metro/models/line_status.dart';

class LineStatusApiClient {
  static const baseUrl =
      'https://apim-proximotrem-prd-brazilsouth-001.azure-api.net/api/v1';

  final Dio _dio;
  LineStatusApiClient({Dio? dio, int timeoutSeconds = 10})
      : _dio = dio ?? Dio(BaseOptions(baseUrl: baseUrl, connectTimeout: Duration(seconds: timeoutSeconds), receiveTimeout: Duration(seconds: timeoutSeconds)));

  Future<List<LineStatus>> fetchLines() async {
    final response = await _dio.get<Map<String, dynamic>>('/lines');
    final data = response.data?['Data'];
    if (data is! List) {
      throw const FormatException('Resposta inesperada da API de status');
    }
    return data.map((e) => LineStatus.fromJson(
        (e as Map<String, dynamic>)['Code'] as int, e)).toList();
  }
}
```

- [ ] **Step 4: Implementar o repositório**

`lib/data/repositories/status_repository.dart`:

```dart
import 'package:seu_metro/data/status_api/line_status_api_client.dart';
import 'package:seu_metro/models/line_status.dart';

class StatusRepository {
  final LineStatusApiClient client;
  final Duration cacheDuration;
  List<LineStatus>? _cache;
  DateTime? _lastFetched;

  StatusRepository({required this.client, this.cacheDuration = const Duration(minutes: 5)});

  List<LineStatus>? get cache => _cache;
  DateTime? get lastFetched => _lastFetched;

  Future<List<LineStatus>> getStatuses() async {
    final cache = _cache;
    final last = _lastFetched;
    if (cache != null && last != null && DateTime.now().difference(last) < cacheDuration) {
      return cache;
    }
    try {
      final fresh = await client.fetchLines();
      _cache = fresh;
      _lastFetched = DateTime.now();
      return fresh;
    } catch (_) {
      if (cache != null) return cache;
      rethrow;
    }
  }
}
```

- [ ] **Step 5: Rodar os testes até passar**

Run: `flutter test test/data/status_repository_test.dart test/data/line_status_api_client_test.dart`
Expected: PASS. Se o teste de rede real falhar por rede indisponível no ambiente de CI, rode apenas o teste do fake (`status_repository_test.dart`) e documente; o teste de rede real deve ser `skip` quando offline:
```dart
final live = !const bool.fromEnvironment('skipLiveApi');
test('...', () async { ... }, skip: live ? false : 'API offline');
```

- [ ] **Step 6: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: status ao vivo via API pública com cache"
```

---

### Task 8: Favoritos locais

**Files:**
- Create: `lib/data/repositories/favorites_repository.dart`
- Test: `test/data/favorites_repository_test.dart`

**Interfaces:**
- Produces: `class FavoritesRepository { Future<List<String>> getFavorites(); Future<bool> isFavorite(String stationId); Future<void> toggle(String stationId); }` — guarda em `shared_preferences` sob a chave `favorites_station_ids` (lista de ids).

- [ ] **Step 1: Escrever o teste (failing)**

`test/data/favorites_repository_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/data/repositories/favorites_repository.dart';
import 'package:shared_preferences/shared_preferences.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  test('toggle adiciona e remove favorito', () async {
    SharedPreferences.setMockInitialValues({});
    final repo = FavoritesRepository();
    expect(await repo.isFavorite('luz'), isFalse);
    await repo.toggle('luz');
    expect(await repo.isFavorite('luz'), isTrue);
    expect(await repo.getFavorites(), ['luz']);
    await repo.toggle('luz');
    expect(await repo.isFavorite('luz'), isFalse);
  });

  test('persiste favoritos entre instâncias', () async {
    SharedPreferences.setMockInitialValues({});
    final repo = FavoritesRepository();
    await repo.toggle('se');
    final repo2 = FavoritesRepository();
    expect(await repo2.isFavorite('se'), isTrue);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/data/favorites_repository_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `lib/data/repositories/favorites_repository.dart`**

```dart
import 'package:shared_preferences/shared_preferences.dart';

class FavoritesRepository {
  static const _key = 'favorites_station_ids';

  Future<List<String>> getFavorites() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getStringList(_key) ?? [];
  }

  Future<bool> isFavorite(String stationId) async {
    return (await getFavorites()).contains(stationId);
  }

  Future<void> toggle(String stationId) async {
    final prefs = await SharedPreferences.getInstance();
    final current = prefs.getStringList(_key) ?? [];
    if (current.contains(stationId)) {
      await prefs.setStringList(_key, current.where((id) => id != stationId).toList());
    } else {
      await prefs.setStringList(_key, [...current, stationId]);
    }
  }
}
```

- [ ] **Step 4: Rodar o teste para passar**

Run: `flutter test test/data/favorites_repository_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: repositório de favoritos locais"
```

---

### Task 9: Providers Riverpod + tema + shell real

**Files:**
- Modify: `lib/main.dart`, `lib/features/home/home_shell.dart`
- Create: `lib/providers/repositories.dart`, `lib/providers/status_provider.dart`, `lib/theme/line_colors.dart`
- Test: `test/providers/repositories_test.dart`

**Interfaces:**
- Consumes: repositórios (Tasks 4, 7, 8), pathfinding (Task 5), `MetroGraph`.
- Produces:
  - `final lineRepositoryProvider = Provider<LineRepository>((ref) => LineRepository());`
  - `final stationRepositoryProvider = Provider<StationRepository>(...);`
  - `final scheduleRepositoryProvider = Provider<ScheduleRepository>(...);`
  - `final favoritesRepositoryProvider = Provider<FavoritesRepository>(...);`
  - `final metroGraphProvider = FutureProvider<MetroGraph>((ref) async { ... });`
  - `final statusProvider = FutureProvider<StatusSnapshot>((ref) async { ... });` — em sucesso retorna `StatusSnapshot(data, updatedAt, isStale: false)`; em erro com cache retorna `StatusSnapshot(cache, updatedAt: repo.lastFetched, isStale: true)`; em erro sem cache re-lança (UI mostra erro). `ref.invalidate(statusProvider)` para pull-to-refresh.
  - `class StatusSnapshot { final List<LineStatus> data; final DateTime updatedAt; final bool isStale; }`
  - `final statusRepositoryProvider = Provider<StatusRepository>((ref) => StatusRepository(client: LineStatusApiClient()));`
  - `final stationsProvider = FutureProvider<List<Station>>(...);`
  - `final linesProvider = FutureProvider<Map<String, Line>>(...);`
  - `final schedulesProvider = FutureProvider<List<LineSchedule>>(...);`
  - `final favoritesProvider = StateNotifierProvider<FavoritesNotifier, Set<String>>(...)` com método `toggle(String id)`.
  - `class LineColors { static int colorFor(String lineId); }` — map id→cor oficial (Task 3) + fallback cinza `0xFF757575`.

- [ ] **Step 1: Escrever o teste (failing)**

`test/providers/repositories_test.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/providers/repositories.dart';

void main() {
  test('stationsProvider carrega estações', () async {
    final container = ProviderContainer();
    addTearDown(container.dispose);
    final stations = await container.read(stationsProvider.future);
    expect(stations.length, greaterThanOrEqualTo(100));
  });

  test('metroGraphProvider monta grafo conectado', () async {
    final container = ProviderContainer();
    addTearDown(container.dispose);
    final graph = await container.read(metroGraphProvider.future);
    expect(graph.plan('luz', 'santo_amaro'), isNotNull);
  });

  test('LineColors retorna cor oficial da linha 1', () {
    expect(LineColors.colorFor('1'), 0xFF00378C);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/providers/repositories_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar providers, tema e notifier**

`lib/providers/repositories.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:seu_metro/data/repositories/favorites_repository.dart';
import 'package:seu_metro/data/repositories/line_repository.dart';
import 'package:seu_metro/data/repositories/schedule_repository.dart';
import 'package:seu_metro/data/repositories/station_repository.dart';
import 'package:seu_metro/models/line.dart';
import 'package:seu_metro/models/line_schedule.dart';
import 'package:seu_metro/models/station.dart';
import 'package:seu_metro/services/pathfinding/metro_graph.dart';

final lineRepositoryProvider = Provider<LineRepository>((ref) => LineRepository());
final stationRepositoryProvider = Provider<StationRepository>((ref) => StationRepository());
final scheduleRepositoryProvider = Provider<ScheduleRepository>((ref) => ScheduleRepository());
final favoritesRepositoryProvider = Provider<FavoritesRepository>((ref) => FavoritesRepository());

final stationsProvider = FutureProvider<List<Station>>((ref) async {
  return ref.watch(stationRepositoryProvider).getAll();
});

final linesProvider = FutureProvider<Map<String, Line>>((ref) async {
  return ref.watch(lineRepositoryProvider).byId();
});

final schedulesProvider = FutureProvider<List<LineSchedule>>((ref) async {
  return ref.watch(scheduleRepositoryProvider).getAll();
});

final metroGraphProvider = FutureProvider<MetroGraph>((ref) async {
  final lines = await ref.watch(lineRepositoryProvider).getAll();
  final stations = await ref.watch(stationRepositoryProvider).getAll();
  return MetroGraph.build(lines, stations);
});

final favoritesProvider =
    StateNotifierProvider<FavoritesNotifier, Set<String>>(
        (ref) => FavoritesNotifier(ref.watch(favoritesRepositoryProvider)));
```

`FavoritesNotifier` (mesmo arquivo):

```dart
class FavoritesNotifier extends StateNotifier<Set<String>> {
  final FavoritesRepository _repo;
  FavoritesNotifier(this._repo) : super(const {}) {
    _load();
  }
  Future<void> _load() async {
    state = (await _repo.getFavorites()).toSet();
  }
  Future<void> toggle(String stationId) async {
    await _repo.toggle(stationId);
    state = (await _repo.getFavorites()).toSet();
  }
}
```

`lib/providers/status_provider.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:seu_metro/data/repositories/status_repository.dart';
import 'package:seu_metro/data/status_api/line_status_api_client.dart';
import 'package:seu_metro/models/line_status.dart';

class StatusSnapshot {
  final List<LineStatus> data;
  final DateTime updatedAt;
  final bool isStale;
  const StatusSnapshot({
    required this.data,
    required this.updatedAt,
    required this.isStale,
  });
}

final statusRepositoryProvider =
    Provider<StatusRepository>((ref) => StatusRepository(client: LineStatusApiClient()));

final statusProvider = FutureProvider<StatusSnapshot>((ref) async {
  final repo = ref.watch(statusRepositoryProvider);
  try {
    final data = await repo.getStatuses();
    return StatusSnapshot(
      data: data,
      updatedAt: repo.lastFetched ?? DateTime.now(),
      isStale: false,
    );
  } catch (_) {
    final cached = repo.cache;
    if (cached != null) {
      return StatusSnapshot(
        data: cached,
        updatedAt: repo.lastFetched ?? DateTime.now(),
        isStale: true,
      );
    }
    rethrow;
  }
});
```

`lib/theme/line_colors.dart`:

```dart
import 'package:seu_metro/models/line.dart';

class LineColors {
  static const Map<String, int> official = {
    '1': 0xFF00378C, '2': 0xFF186D55, '3': 0xFFF51200, '4': 0xFFEFBA00,
    '5': 0xFF9271B1, '6': 0xFFF27C00, '7': 0xFFC80857, '8': 0xFF949488,
    '9': 0xFF00AA80, '10': 0xFF00998C, '11': 0xFFE33825, '12': 0xFF193E81,
    '13': 0xFF2D9F7E, '15': 0xFF9D968D, '17': 0xFFD0A238,
  };
  static const _fallback = 0xFF757575;
  static int colorFor(String lineId) => official[lineId] ?? _fallback;
}
```

`FavoritesNotifier`:

```dart
class FavoritesNotifier extends StateNotifier<Set<String>> {
  final FavoritesRepository _repo;
  FavoritesNotifier(this._repo) : super(const {}) {
    _load();
  }
  Future<void> _load() async {
    state = (await _repo.getFavorites()).toSet();
  }
  Future<void> toggle(String stationId) async {
    await _repo.toggle(stationId);
    state = (await _repo.getFavorites()).toSet();
  }
}
```

- [ ] **Step 4: Rodar o teste para passar**

Run: `flutter test test/providers/repositories_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: providers riverpod e cores oficiais das linhas"
```

---

### Task 10: Tela de Mapa

**Files:**
- Modify: `lib/features/map/map_page.dart`
- Create: `lib/features/map/station_bottom_sheet.dart`
- Test: `test/widget/map_page_test.dart`

**Interfaces:**
- Consumes: `stationsProvider`, `linesProvider`, `metroGraphProvider`, `favoritesProvider`, `LineColors`, `geolocator`, `nearestStation`.
- Produces: `MapPage` (ConsumerWidget) com mapa flutter_map, marcadores por estação, bottom sheet, toggle de camadas (Metrô/CPTM) e botão de localização.

- [ ] **Step 1: Escrever o widget test (failing)**

`test/widget/map_page_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/map/map_page.dart';

void main() {
  testWidgets('MapPage renderiza o mapa e estações', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: MaterialApp(home: MapPage())));
    await tester.pumpAndSettle();
    expect(find.text('Seu Metrô'), findsNothing); // sem título fixo
    expect(find.byType(FlutterMap), findsWidgets);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/widget/map_page_test.dart`
Expected: FAIL (não compila / FlutterMap ausente).

- [ ] **Step 3: Implementar `lib/features/map/map_page.dart`**

Usar `flutter_map` 8.x:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_map/flutter_map.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:geolocator/geolocator.dart';
import 'package:latlong2/latlong.dart';
import 'package:seu_metro/features/map/station_bottom_sheet.dart';
import 'package:seu_metro/models/station.dart';
import 'package:seu_metro/providers/repositories.dart';
import 'package:seu_metro/services/location/nearest_station.dart';
import 'package:seu_metro/theme/line_colors.dart';

class MapPage extends ConsumerStatefulWidget {
  const MapPage({super.key});
  @override
  ConsumerState<MapPage> createState() => _MapPageState();
}

class _MapPageState extends ConsumerState<MapPage> {
  final _controller = MapController();
  bool _showMetro = true;
  bool _showCptm = true;
  Station? _selected;
  int _offscreenCount = 0;

  @override
  Widget build(BuildContext context) {
    final stationsAsync = ref.watch(stationsProvider);
    return Scaffold(
      body: stationsAsync.when(
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('Não foi possível carregar as estações.')),
        data: (stations) => _buildMap(context, stations),
      ),
      floatingActionButton: _locateButton(),
    );
  }

  Widget _buildMap(BuildContext context, List<Station> stations) {
    final visible = stations.where((s) {
      final metro = s.lineIds.any((l) => const {'1','2','3','4','5','6','15','17'}.contains(l));
      return (_showMetro && metro) || (_showCptm && !metro);
    }).toList();
    return Stack(children: [
      FlutterMap(
        mapController: _controller,
        options: MapOptions(
          initialCenter: const LatLng(-23.55, -46.633),
          initialZoom: 12,
          interactionOptions: const InteractionOptions(flags: InteractiveFlag.all & ~InteractiveFlag.rotate),
          onPositionChanged: (pos, hasGesture) {
            final bounds = pos.visibleBounds;
            setState(() {
              _offscreenCount = visible
                  .where((s) => !bounds.containsLatLng(LatLng(s.lat, s.lon)))
                  .length;
            });
          },
        ),
        children: [
          TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png',
            userAgentPackageName: 'br.com.seumetro'),
          MarkerLayer(markers: visible.map((s) => Marker(
            point: LatLng(s.lat, s.lon),
            width: 22, height: 22,
            child: GestureDetector(
              onTap: () => setState(() => _selected = s),
              child: _stationMarker(s),
            ),
          )).toList()),
        ],
      ),
      Positioned(top: 16, right: 16, child: _layerToggles()),
      if (_offscreenCount > 0)
        Positioned(left: 16, bottom: 16, child: _offscreenBadge()),
    ]);
  }

  Widget _offscreenBadge() {
    return Card(
      child: Padding(
        padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
        child: Text('$_offscreenCount estações fora da tela'),
      ),
    );
  }

  Widget _stationMarker(Station s) {
    final color = Color(LineColors.colorFor(s.lineIds.first));
    return Container(
      decoration: BoxDecoration(shape: BoxShape.circle, color: color, border: Border.all(color: Colors.white, width: 2)),
      padding: EdgeInsets.all(s.lineIds.length > 1 ? 2 : 4),
    );
  }

  Widget _layerToggles() {
    return Card(child: Padding(
      padding: const EdgeInsets.all(8),
      child: Column(mainAxisSize: MainAxisSize.min, children: [
        SwitchListTile(title: const Text('Metrô'), value: _showMetro, onChanged: (v) => setState(() => _showMetro = v), dense: true),
        SwitchListTile(title: const Text('CPTM'), value: _showCptm, onChanged: (v) => setState(() => _showCptm = v), dense: true),
      ]),
    ));
  }

  Widget _locateButton() {
    return FloatingActionButton(
      onPressed: () => _locate(),
      child: const Icon(Icons.my_location),
    );
  }

  Future<void> _locate() async {
    var permission = await Geolocator.checkPermission();
    if (permission == LocationPermission.denied) {
      permission = await Geolocator.requestPermission();
    }
    if (permission == LocationPermission.denied ||
        permission == LocationPermission.deniedForever) {
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('Permissão de localização negada. Use a busca de estação.')),
        );
      }
      return;
    }
    final position = await Geolocator.getCurrentPosition();
    final stations = await ref.read(stationsProvider.future);
    final nearest = nearestStation(stations, position.latitude, position.longitude);
    if (!mounted) return;
    if (nearest != null) {
      _controller.move(LatLng(nearest.lat, nearest.lon), 15);
      setState(() => _selected = nearest);
      showStationSheet(context, nearest);
    }
  }
}
```

- [ ] **Step 4: Bottom sheet da estação**

`lib/features/map/station_bottom_sheet.dart` — `showStationSheet(BuildContext, Station)` com: nome, chips das linhas (cor), botões "Ver horários", "Como chegar" (navega para a aba Rotas pré-preenchida — v1: navega para a tela de rotas), e estrela de favorito conectada a `favoritesProvider`. Exibir na `MapPage` quando `_selected != null` via `showModalBottomSheet`.

- [ ] **Step 5: Rodar os testes até passar**

Run: `flutter test test/widget/map_page_test.dart`
Expected: PASS (o teste de smoke deve renderizar o FlutterMap; ajuste o matcher se o `flutter_map` não expor `FlutterMap` por tipo — use `find.byType(FlutterMap)` conforme a API da versão instalada).

- [ ] **Step 6: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: tela de mapa com estações e bottom sheet"
```

---

### Task 11: Tela de Rotas

**Files:**
- Modify: `lib/features/routes/routes_page.dart`
- Create: `lib/features/routes/station_picker.dart`, `lib/features/routes/route_result_card.dart`
- Test: `test/widget/routes_page_test.dart`

**Interfaces:**
- Consumes: `stationsProvider`, `metroGraphProvider`, `linesProvider`, `schedulesProvider`.
- Produces: `RoutesPage` (ConsumerStatefulWidget) com 2 campos de autocomplete (Origem/Destino) e exibição do `RoutePlan` (pernas com linha/cor, direção, nº de estações, tempo; passos de baldeação).

- [ ] **Step 1: Escrever o widget test (failing)**

`test/widget/routes_page_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/routes/routes_page.dart';

void main() {
  testWidgets('RoutesPage permite escolher origem e destino e mostra rota', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: MaterialApp(home: RoutesPage())));
    await tester.pumpAndSettle();
    expect(find.text('Origem'), findsOneWidget);
    expect(find.text('Destino'), findsOneWidget);
    // digita origem
    await tester.enterText(find.byType(TextField).first, 'Luz');
    await tester.pumpAndSettle();
    await tester.tap(find.text('Luz').first);
    await tester.pumpAndSettle();
    // digita destino
    await tester.enterText(find.byType(TextField).last, 'Santo Amaro');
    await tester.pumpAndSettle();
    await tester.tap(find.text('Santo Amaro').last);
    await tester.pumpAndSettle();
    await tester.tap(find.widgetWithText(FilledButton, 'Calcular rota'));
    await tester.pumpAndSettle();
    expect(find.textContaining('Linha'), findsWidgets);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/widget/routes_page_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `RoutesPage`**

UI: `TextField` de Origem com autocomplete (lista filtrada por prefixo, ignorando acento via `toLowerCase` e remoção de acentos com `String.fromCharCodes` + normalize se disponível; v1: `toLowerCase` apenas), idem Destino; botão `FilledButton('Calcular rota')`; ao clicar, `metroGraphProvider` → `plan(from, to)` e exibe `RouteResultCard` com cada perna (cor da linha, nome, direção "Sentido X", nº de estações, minutos) e a lista de baldeações ("Baldear em Luz").

Tratamento de "sem caminho": quando `plan(from, to)` retorna `null`, exibir no lugar do resultado: `Não há rota disponível entre essas estações.`

- [ ] **Step 4: Rodar o teste até passar**

Run: `flutter test test/widget/routes_page_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: planejador de rotas estação a estação"
```

---

### Task 12: Tela de Status ao vivo

**Files:**
- Modify: `lib/features/status/status_page.dart`
- Test: `test/widget/status_page_test.dart`

**Interfaces:**
- Consumes: `statusProvider`, `linesProvider`, `LineColors`.
- Produces: `StatusPage` (ConsumerWidget) com lista de linhas e chips de status; refresh automático (Timer a cada 5 min no `initState` que chama `ref.invalidate(statusProvider)`) + `RefreshIndicator` (pull-to-refresh); rótulo "atualizado há X min"; banner offline em caso de erro.

- [ ] **Step 1: Escrever o widget test (failing)**

`test/widget/status_page_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/status/status_page.dart';
import 'package:seu_metro/models/line_status.dart';
import 'package:seu_metro/providers/status_provider.dart';

void main() {
  testWidgets('StatusPage lista linhas com status', (tester) async {
    final snapshot = StatusSnapshot(
      data: [LineStatus(lineId: '1', statusCode: 'OperacaoNormal', statusLabel: 'Operação Normal', statusColor: 'verde', description: null, updatedAt: DateTime.now())],
      updatedAt: DateTime.now(),
      isStale: false,
    );
    final override = statusProvider.overrideWith((ref) => Future.value(snapshot));
    await tester.pumpWidget(ProviderScope(overrides: [override], child: const MaterialApp(home: StatusPage())));
    await tester.pumpAndSettle();
    expect(find.textContaining('Operação Normal'), findsWidgets);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/widget/status_page_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `StatusPage`**

Mapeamento statusColor → cor: `verde`→`Colors.green`, `amarelo`→`Colors.amber`, `vermelho`→`Colors.red`, `cinza`→`Colors.grey`. `StatusPage` usa `ref.watch(statusProvider)` (`AsyncValue<StatusSnapshot>`):
- `data`: mostra a lista; rótulo "atualizado há X min" via `snapshot.updatedAt`.
- `data` com `snapshot.isStale`: além da lista, banner "Sem conexão — mostrando últimos dados conhecidos".
- `error`: `RefreshIndicator` + mensagem "Não foi possível obter o status. Puxe para tentar novamente."

**Estação mais próxima (spec)**: no topo da `StatusPage`, um card "Estação mais próxima" com um botão que usa `geolocator` + `nearestStation` (Task 6) e exibe `nome da estação` + `~X km de distância` + botão "Como chegar" (navega para a aba Rotas). O cálculo usa `stationsProvider`. Sem permissão de GPS, o card mostra "Ative a localização para encontrar a estação mais próxima" com botão de tentar novamente.

- [ ] **Step 4: Rodar o teste até passar**

Run: `flutter test test/widget/status_page_test.dart`
Expected: PASS (o teste usa `statusProvider.overrideWith` para não depender de rede).

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: status ao vivo das linhas com refresh"
```

---

### Task 13: Tela de Horários

**Files:**
- Modify: `lib/features/schedules/schedules_page.dart`
- Test: `test/widget/schedules_page_test.dart`

**Interfaces:**
- Consumes: `stationsProvider`, `schedulesProvider`, `linesProvider`.
- Produces: `SchedulesPage` (ConsumerStatefulWidget) com seletor de estação (autocomplete) e tabela por linha/direção: primeiro trem, último trem, intervalo pico/normal. Se a hora atual estiver fora do intervalo, exibir "Estação fechada".

- [ ] **Step 1: Escrever o widget test (failing)**

`test/widget/schedules_page_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/schedules/schedules_page.dart';

void main() {
  testWidgets('SchedulesPage mostra horários da estação', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: MaterialApp(home: SchedulesPage())));
    await tester.pumpAndSettle();
    await tester.enterText(find.byType(TextField).first, 'Luz');
    await tester.pumpAndSettle();
    await tester.tap(find.text('Luz').first);
    await tester.pumpAndSettle();
    expect(find.textContaining('Primeiro trem'), findsWidgets);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/widget/schedules_page_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `SchedulesPage`**

Para a estação selecionada, listar suas linhas; para cada linha mostrar as 2 direções (`schedulesProvider` filtrado por `lineId`), tabela com `Primeiro trem`, `Último trem`, `Intervalo (pico)`, `Intervalo (normal)`. Indicador "Estação fechada" quando `now` fora de `[firstTrain, lastTrain+1h]` (usando hora de hoje).

- [ ] **Step 4: Rodar o teste até passar**

Run: `flutter test test/widget/schedules_page_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: horários tabelados por estação"
```

---

### Task 14: Tela de Favoritos

**Files:**
- Modify: `lib/features/favorites/favorites_page.dart`
- Test: `test/widget/favorites_page_test.dart`

**Interfaces:**
- Consumes: `favoritesProvider`, `stationsProvider`, `metroGraphProvider`.
- Produces: `FavoritesPage` (ConsumerWidget) listando estações favoritas com atalhos "Ver no mapa" (navega para a aba Mapa centralizada na estação), "Como chegar" (navega para a aba Rotas), e remoção pela estrela. Estado vazio → mensagem "Nenhuma estação favorita ainda".

- [ ] **Step 1: Escrever o widget test (failing)**

`test/widget/favorites_page_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:seu_metro/features/favorites/favorites_page.dart';
import 'package:seu_metro/providers/repositories.dart';

void main() {
  testWidgets('FavoritesPage mostra estado vazio', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: MaterialApp(home: FavoritesPage())));
    await tester.pumpAndSettle();
    expect(find.textContaining('Nenhuma estação'), findsOneWidget);
  });
}
```

- [ ] **Step 2: Rodar para confirmar falha**

Run: `flutter test test/widget/favorites_page_test.dart`
Expected: FAIL.

- [ ] **Step 3: Implementar `FavoritesPage`**

`ref.watch(favoritesProvider)` (Set<String>) → map para `Station` via `stationsProvider`; `ListView` de cards; botão estrela chamando `ref.read(favoritesProvider.notifier).toggle(id)`; navegação entre abas usando um `HomeShellController` simples: elevar um `ValueNotifier<int>`/provider `selectedTabProvider` em `HomeShell` para que MapPage/RoutesPage possam trocar de aba e pré-selecionar estação.

- [ ] **Step 4: Rodar o teste até passar**

Run: `flutter test test/widget/favorites_page_test.dart`
Expected: PASS

- [ ] **Step 5: Analisar e commitar**

```powershell
git add -A
git commit -m "feat: favoritos com navegação entre abas"
```

---

### Task 15: Verificação final

**Files:**
- Modify: nenhum (somente verificação).

- [ ] **Step 1: Analisar**

Run: `flutter analyze`
Expected: No issues found.

- [ ] **Step 2: Rodar todos os testes**

Run: `flutter test`
Expected: TODOS os testes passam.

- [ ] **Step 3: Build de depuração Android**

Run: `flutter build apk --debug`
Expected: BUILD SUCCESSFUL (pode demorar alguns minutos).

- [ ] **Step 4: Smoke manual (opcional)**

Se houver dispositivo/emulador: `flutter run` e conferir mapa, rotas Luz→Santo Amaro, status, horários, favoritos.

- [ ] **Step 5: Commit final**

```powershell
git add -A
git commit -m "chore: verificação final v1"
```

---

## Notas de implementação

- **flutter_map 8.x**: `MapController`, `TileLayer`, `MarkerLayer`, `Marker`, `MapOptions(initialCenter: LatLng(...))`. A API mudou entre versões; confira a versão instalada em `pubspec.lock` e ajuste os nomes se necessário.
- **geolocator**: para o botão de localização, use `Geolocator.checkPermission()`/`requestPermission()` e `getCurrentPosition()`. Em emulador, injete posição de teste.
- **Estações de baldeação**: uma entrada única em `stations.json` com `lines` listando todas as linhas; a topologia (ordem de viagem) vive em `lines.json` → `stations`. A contagem é 166 ids únicos.
- **Ícones de tabs**: Mapa/Rotas/Status/Horários/Favoritos conforme Task 1.
- **Não adicionar comentários ao código** (regra do projeto).
- Todo texto visível ao usuário em pt-BR.
